name: 5-minute-cron
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Update Brains Data
        run: |
          curl --request POST \
          --url 'https://asm-brains-checker.vercel.app/api/brains'
